export type FixScheduleList = {
  startDate: string;
  endDate: string;
  firstDepth: string;
  secondDepth: string;
  memo: string;
  scheduleSid: number;
};

export type CreateScheduleList = {
  startDate: string;
  endDate: string;
  firstDepth: string;
  secondDepth: string;
  memo: string;
};
